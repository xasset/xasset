namespace xasset.example
{
    public enum ExampleScene
    {
        Splash,
        OpeningDialog,
        UpdateVersions,
        Menu,
        LoadAsset,
        LoadAdditiveScene,
        LoadRawAsset,
        Download
    }
}