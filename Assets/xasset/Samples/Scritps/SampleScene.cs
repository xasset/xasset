namespace xasset.samples
{
    public enum SampleScene
    {
        Splash,
        OpeningDialog,
        CheckForUpdates,
        Menu,
        LoadAsset,
        LoadAdditiveScene,
        Download
    }
}