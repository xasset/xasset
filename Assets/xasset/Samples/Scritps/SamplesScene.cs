namespace xasset.samples
{
    public enum SamplesScene
    {
        Splash,
        OpeningDialog,
        CheckForUpdates,
        Menu,
        LoadAsset,
        LoadAdditiveScene,
        Download,
    }
}